
* Unregister [package names...]

Recursively unregister this package and things that depend on it

* MultiVersion

List packages that have more than one version installed

* Outdated

List packages that are behind the latest cabal version
